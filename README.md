# Lean hello world

A minimal Lean 4 development environment using Nix flakes with `nixpkgs`
managed by [npins](https://github.com/andir/npins). The flake has no flake
inputs.

Run the program (with Nix and `just` installed):

```console
just run
```

Expected output:

```text
Hello, world!
```
